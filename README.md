# About

Program creates a sentiment analysis of a movie reviews.

# Requirements

* `python 3.7`
* `make` - You can install by `brew install make`.
* Other python depencies. Type `make dependecies`

# Running the program

To run the program type `make`.
